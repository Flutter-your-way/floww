# floww

A new Flutter project.

## Getting Started

This project is a starting point for a Flutter application.

A few resources to get you started if this is your first Flutter project:

- [Learn Flutter](https://docs.flutter.dev/get-started/learn-flutter)
- [Write your first Flutter app](https://docs.flutter.dev/get-started/codelab)
- [Flutter learning resources](https://docs.flutter.dev/reference/learning-resources)

For help getting started with Flutter development, view the
[online documentation](https://docs.flutter.dev/), which offers tutorials,
samples, guidance on mobile development, and a full API reference.

## Feature Notes

- The "Meet WAVE" intro and "Three Modes" onboarding-intro screens are merged into a single view (`lib/core/auth/views/meet_waves_view.dart`). Tapping "Get Started" plays a custom staggered scale + fade transition (`lib/core/auth/widgets/step_reveal_item.dart`) between the two steps instead of navigating to a second route; the header back arrow reverses it. The old `welcomeToFlowView` route and `WelcomeToFlowView` screen were removed since nothing else referenced them.
- Added an "Account Setup" screen (`lib/core/auth/views/auth_view.dart`, route `AppRouter.accountSetup`) between the mode showcase and onboarding. "Skip for Now" still just proceeds to onboarding as a guest. Google and Apple (iOS only, via `Platform.isIOS`) sign-in are now real: `AuthService` (`lib/core/auth/services/auth_service.dart`) signs in through `firebase_auth` (Google via `google_sign_in` v7's `GoogleSignIn.instance`/`.authenticate()`, Apple via `sign_in_with_apple` with a SHA-256-hashed nonce), then reads/creates a `users/{uid}` Cloud Firestore doc to tell new vs. existing users apart and persist the `UserModel`. `AuthViewModel` (`lib/core/auth/view_models/auth_view_model.dart`) exposes per-provider loading flags and an error message, driving `CustomButton`/`CustomOutlinedButton`'s built-in loading/disabled state; a failure shows inline error text above the buttons. On success, the view routes via `AppRouter.routeAfterAuth(user)` — onboarding questions, Connect Wearables, or Home depending on `UserModel.onboardingCompleted`/`answersSubmitted` (see below) — using a new `NavigationService.pushAndRemoveUntil` (clears the back stack). `SplashView` now checks for an existing session the same way before deciding where to land. Notification permission (`FirebaseMessaging.instance.requestPermission`) is requested once `AuthView` loads, and the FCM token is registered into the user's Firestore doc after a successful sign-in. `CustomButton`/`CustomOutlinedButton` gained optional `leading`/`foregroundColor` params to support this screen's icon buttons.
- Built the Home screen (`lib/core/home/views/home_view.dart`, `HomeProvider` in `core/home/providers/`, section widgets in `core/home/widgets/`): Flow Score, Today's Habit, Today's Workout, Nutrition Summary, Apple Health Sync, Today's Progress, and Muscle Recovery cards, plus a conditional "How to Increase Your Flow Score" card (shown only at 0% flow score) and a Wave Insight banner (shown only when present). Ships with populated sample data by default, but every card's empty-state branch is driven by real (currently zero/empty) values, not dead code.
- Added `MuscleRecoveryCard` (`lib/core/home/widgets/muscle_recovery_card.dart`), shown below Today's Progress: front/back anatomical diagrams (`AppImages.muscleFront`/`muscleBack`, rendered as-is via `SvgPicture` — the highlighted-muscle coloring is baked into the SVGs themselves, not applied at runtime) plus a 2×2 stat grid (days since last workout, in recovery, ready muscles, fatigued muscles). Fixed `AppImages.muscleFront`/`muscleBack`, which previously pointed at `assets/icons/` (a non-existent path for these files) — added `AppImages._imagesBase` and repointed them at `assets/images/`, where the files actually live.
- Added the app shell (`lib/navigation/views/main_tab_view.dart`): a floating bottom tab bar over a `PageTransitionSwitcher` of 5 tabs (Home, Nutrition, Activity, Plans, Stats). Tab switches use a 300ms `FadeThroughTransition` from the `animations` package, filled with `context.colors.backgroundPrimary`; because only the selected tab is mounted, tab screens do not keep state across switches (state that must survive belongs in a provider). `AppBottomNavBar` (`lib/navigation/widgets/app_bottom_nav_bar.dart`) renders the 5 tabs as a pill with a circular selection indicator that slides between tabs via `AnimatedAlign`, plus a per-icon color crossfade. `WaveOrbButton` (`lib/navigation/widgets/wave_orb_button.dart`) is a press-animated stub for the floating WAVE AI orb — `onTap` is currently a no-op placeholder; wiring it to a real voice/AI screen is a follow-up. Nutrition/Activity/Plans/Stats currently render a shared `FeaturePlaceholderView` (`lib/config/widgets/placeholders/feature_placeholder_view.dart`) with a "Coming soon" message until those features are built. `AppRouter.home` now routes to `MainTabView` instead of `HomeView` directly.
- Onboarding now actually submits: `OnboardingDetailsEntity` (`lib/config/entities/onboarding_details_entity.dart`, previously unused) is built from the answers map and written to a new `onboarding_details/{uid}` Cloud Firestore collection (`AppCollection.onboardingDetails`) by the new `OnboardingService` (`lib/core/onboarding/services/onboarding_service.dart`), branching into exactly one of the gym/calisthenics/yoga detail blocks based on the selected training type. `OnboardingProvider.nextQuestion` is now async: completing the last question (`summary_screen`) submits the answers, sets `UserModel.answersSubmitted = true`, and — on success — `OnboardingQuestionView` navigates to a new `ConnectWearablesView` (`lib/core/onboarding/views/connect_wearables_view.dart`, route `AppRouter.connectWearables`). "Connect wearables" was removed from the question data entirely (was `apple_health` in `phase_5`) so it's a genuine final step outside the questionnaire; `HealthIntegrationWidget` was decoupled from `OnboardingProvider` (now takes `isConnected`/`onToggle`) so it's reusable standalone there. Finishing that screen sets `UserModel.onboardingCompleted = true` and goes to Home. `AppRouter.routeAfterAuth(user)` is the single place `SplashView`/`AuthView` use to decide between onboarding questions, Connect Wearables, and Home based on those two flags — so closing the app between submission and finishing the wearables step resumes exactly there instead of replaying the questionnaire.
- Added `HapticManager` (`lib/config/utils/haptics/haptic_manager.dart`), a static wrapper around the `haptic_feedback` package. Call `HapticManager.success()/.warning()/.error()/.light()/.medium()/.heavy()/.rigid()/.soft()/.selection()` from anywhere — each checks (and caches) `Haptics.canVibrate()` first and no-ops silently on unsupported platforms/devices. Not yet wired into any interaction; hook it into buttons/taps as needed.
- Apple Health is now a real integration, not a toggle. New feature module `lib/core/health/`: `HealthService` (`services/health_service.dart`) wraps the `health` package (HealthKit on iOS, Health Connect on Android) and owns the read-only type list (steps, active energy, heart rate, resting heart rate, sleep, workouts), permission requests, and a `fetchTodaySnapshot()` that returns a `HealthSnapshot` (`models/health_snapshot.dart`) with today's steps, active kcal, resting HR, sleep minutes and workout count; `HealthProvider` (`providers/health_provider.dart`) holds the `HealthConnectionStatus` (disconnected/connecting/connected/unavailable), the snapshot, sync + error state, and persists the connected flag to `SharedPreferences` under `health_connected`, restoring and re-syncing at app start. It is registered app-wide in `main.dart`. `HealthIntegrationWidget` now takes `isConnected`/`isConnecting`/`statusLabel`/`onConnect` — tapping the Apple Health row triggers the real system permission sheet, the pill shows a spinner while connecting and "Connected" after, and the synced summary (e.g. "8,432 steps · 7h 12m sleep") renders under the title. `ConnectWearablesView` writes the real granted state through `markWearablesStepDone`, and the Home screen's `AppleHealthSyncCard` is wired to the same provider (`connected`/`syncDetail`/`onConnect`/`onDisconnect`), replacing the removed `HomeProvider.healthSyncConnected` mock. Native setup: iOS gets `NSHealthShareUsageDescription`/`NSHealthUpdateUsageDescription` in `Info.plist` plus a new `ios/Runner/Runner.entitlements` (HealthKit capability, wired into all three Runner build configs); Android gets the Health Connect `<queries>`, rationale intent-filter, `ViewPermissionUsageActivity` alias, the six read permissions + `ACTIVITY_RECOGNITION`, and `minSdk = 26` (Health Connect's floor). HealthKit only returns data on a physical device — the iOS Simulator has no Health app. `AppRadius.xl4` (32) and `AppSizes.s120` were added so the widget's app-icon tile uses tokens instead of inline values.
- `AppBackground` (`lib/config/utils/backgrounds/app_background.dart`) gained a `scrollable` flag. When set, it owns the `SingleChildScrollView` itself and translates the background layer up with the scroll offset, so the background scrolls with the content instead of staying fixed; the shift is clamped at 0, so iOS pull-down bounce keeps the background pinned rather than revealing a gap above it. Pass plain (non-scrolling) content as `child` in this mode. The Home screen uses it.
- AI food scan data layer (the capture screen is described below). Backend lives in the sibling repo `/Volumes/T7/code/floww_cloud` (Express on Cloud Functions v2, `POST /food/scan`) — it sends the photo to OpenAI with a strict JSON schema and returns a typed food model: name, description, serving size/weight, confidence, health score, per-ingredient macros, and full nutrition (12 macros plus `waterMl`, 14 vitamins, 11 minerals; units are in the field names — `G`/`Mg`/`Mcg`/`Ml`). The Dart mirror is `FoodModel` (`lib/core/nutrition/models/food_model.dart`), field-for-field identical to the backend's `foodModelSchema`, so the returned object is saved unchanged unless the user edits it first (see the result sheet below). `FoodScanService` (`lib/core/nutrition/services/food_scan_service.dart`) has `scanFood(imageBytes:, mimeType:, note:)` (posts base64 JSON with the Firebase ID token via `dart:io` `HttpClient` — no new package) (saving moved to `NutritionLogService.addFoodLog`, which writes to `users/{uid}/food_logs/{food.id}` under the new `AppCollection.foodLogs` — see the Nutrition feature note). Server errors map to `FoodScanException` with a typed `FoodScanErrorCode` (`notFood`, `quotaExceeded`, `aiUnavailable`, `network`, …). The API base URL is `AppApi.baseUrl` (`lib/config/constants/app_api.dart`), overridable with `--dart-define=API_BASE_URL=...` for the local emulator. `test/fixtures/food_model.json` is generated from the backend's own sanitizer and schema; `test/food_model_test.dart` checks the Dart model round-trips it exactly — regenerate the fixture whenever the backend model changes. Firestore rules live in the backend repo at `floww_cloud/firestore.rules` (deploy with `firebase deploy --only firestore:rules`); they let each user read/write their own `users/{uid}`, `users/{uid}/food_logs` and `onboarding_details/{uid}`, and keep `ai_usage` server-only. A rule on `users/{uid}` does not cover its subcollections, so `food_logs` needs its own block — without it saving a scan fails with `[cloud_firestore/permission-denied]`.
- Built the Nutrition tab (`lib/core/nutrition/views/nutrition_view.dart`) from the "date change / nothing logged" design: header with a working date selector (prev/next arrows, tap the date to open a date picker, range ±365 days), empty-state card, Daily Goal card (calories + segmented protein/carbs/fats bar), a tip card, a Weekly Nutrition Report card (Mon–Sun range of the selected date), and floating Scan / Log Food buttons above the tab bar (Scan opens the AI Food Scan screen; Log Food opens the Log Food sheet — see the full Nutrition feature note below). `NutritionViewModel` moved to `lib/core/nutrition/view_models/nutrition_view_model.dart` and owns all date logic and copy: title reads "Today's" for today and the weekday otherwise, and the empty-state/tip text switches between past, today and future dates. Goals come from `NutritionGoal.defaults` (2,450 kcal / 200g / 310g / 90g) until real targets are computed. Shared pieces extracted so Home and Nutrition use the same widgets: `HomeCard` became `AppCard` (`lib/config/widgets/cards/app_card.dart`, new `glow` variant), the two-line greeting/title became `ScreenTitle` (`lib/config/widgets/headers/screen_title.dart`, now used by `HomeHeader`), and the macro label became `MacroValueLabel` (`lib/config/widgets/nutrition/macro_value_label.dart`, now used by `NutritionSummaryCard`). New tokens: `AppTypography.heading3Medium` (20px, mapped to `textTheme.headlineSmall`), `AppGradientTokens.glowCard` (per mode) and `macroProtein`/`macroCarbs`/`macroFats`. New utils `AppDateUtils` (`lib/config/utils/dates/`) and `NumberFormatter` (`lib/config/utils/formatters/`). New icon `AppImages.foodScanIcon`.
- AI Food Scan screen (`lib/core/nutrition/views/food_scan_view.dart`, route `AppRouter.foodScan`, opened from the Nutrition Scan button). A live back-camera preview fills the whole screen (cover-cropped), with the "AI Food Scan" `CustomHeader`, the white corner-bracket scan frame (`FoodScanFrame`), a dark bottom scrim (new `AppGradientTokens.cameraScrim`), and a shutter button. Tapping the shutter takes the photo, freezes the preview on that frame, and hides the controls. While `FoodScanService.scanFood` runs, a line sweeps up and down across the frame: everything above the line is sharp and everything below is blurred (`FoodScanSweep`). When the scan succeeds, the screen pops and returns the `FoodModel` as the route result without saving it, and the Nutrition tab then opens the AI Food Scan result sheet (below). When it fails, the server's message is shown above the shutter and the live preview resumes so the user can retake. New `camera` package (approved): `FoodCameraService` (`services/food_camera_service.dart`) owns the `CameraController` at `ResolutionPreset.high` (~1280px JPEG, which matches the backend's downscale guidance) with audio disabled, and maps permission/startup/capture failures to `FoodCameraException`. `FoodScanViewModel` (`view_models/food_scan_view_model.dart`) owns the status (`starting`/`ready`/`capturing`/`scanning`/`suspended`/`unavailable`) and the app lifecycle. It releases the camera when the app goes inactive and restarts it on resume. A denied/unavailable camera is retried only after the app has really been in the background (e.g. the user went to Settings), so Android permission dialogs can't cause a retry loop. `NavigationService.pop` now takes an optional result. Native setup: iOS `NSCameraUsageDescription` in `Info.plist`; Android's `CAMERA` permission is merged from the plugin's manifest. The iOS Simulator has no camera, so it shows "No camera found on this device." — test capture on a physical device. Next to the shutter there is a photo-library button (`PhotoLibraryService`, `services/photo_library_service.dart`, new `image_picker` package) that picks a photo instead of taking one and then runs the same scan — this is what makes the flow testable on the Simulator, so it stays enabled (and centred) even when the camera is unavailable. The picker downscales to 1280px at JPEG quality 85, which re-encodes HEIC to the JPEG the backend expects. While a picked photo is being analyzed it replaces the camera background (`FoodScanViewModel.scannedPhoto`, cover-cropped under the scan frame and sweep), so you see what is being scanned; camera captures keep the frozen preview instead. Cancelling restarts the live preview; when there was no camera to begin with, the previous message comes back. It is a normal button in release builds too — gate it behind `kDebugMode` in `FoodScanView` if it should not ship. iOS needs `NSPhotoLibraryUsageDescription` (added); Android's system photo picker needs no permission. `test/food_scan_view_model_test.dart` covers capture/scan and library-pick success and failure plus the lifecycle rules with fake services.
- AI Food Scan result flow (`lib/core/nutrition/views/food_scan_result_sheet.dart`, `FoodScanResultSheet.show`, opened by the Nutrition tab once the scan returns a `FoodModel`). It is a floating sheet over a blurred, dimmed Nutrition screen: a new shared `showAppFloatingSheet` + `AppFloatingSheet` (`lib/config/widgets/sheets/app_floating_sheet.dart`; slide-up transition, keyboard-aware bottom inset, animated height between steps) plus `AppSheetPanel` (`app_sheet_panel.dart`; title + close button, subtitle, scrollable body, footer). **Review** step ("AI Food Scan"): `DetectedMealCard` shows the meal name with "(estimated)", total kcal and its share of the daily goal, Protein/Carbs/Fat tiles, and Fiber/Sugar/Sodium/Water bars against their daily goals (`MicronutrientProgressList`). Buttons: "Edit Meal" and "Start Workout". "Start Workout" keeps the label from the design, but what it does is save the meal to the log. **Edit** step ("Edit Nutrients"): `MealNameField` plus two 2×2 `NutrientInputGrid`s (calories/protein/carbs/fat in the accent color, fiber/sugar/sodium/water in white; decimal keypad, at most 5 digits and 1 decimal). "Back" applies the edits to the review; "Add to Log" saves. `FoodScanResultViewModel` (`view_models/food_scan_result_view_model.dart`) owns all labels and the draft. Only fields the user actually changed are re-parsed, so untouched values keep full precision, and "(estimated)" disappears once something is edited. A blank name disables saving. Saving goes through `NutritionLogService.addFoodLog` to `users/{uid}/food_logs/{id}`, stamped with the Nutrition tab's selected date and a meal type derived from the time of day; failures show inline above the buttons. While saving, the sheet can't be dismissed. The close button discards the result; system back while editing returns to the review. **Water end-to-end:** the backend (`floww_cloud`) gained `macros.waterMl` in its schema and prompt (prompt version `food-scan-v2`), and `MacroNutrients.waterMl` reads a missing value as 0 so older logs still parse. The backend must be redeployed (`npm run deploy` in `floww_cloud/functions`) before scans return real water values; until then the app shows 0 ml. `test/fixtures/food_model.json` was regenerated through the backend's own sanitizer and schema. New: `copyWith` on `FoodModel`/`FoodNutrition`/`MacroNutrients`/`Minerals` for the edited fields; micronutrient goals on `NutritionGoal` (fiber 30 g, sugar 50 g, sodium 2300 mg, water 2500 ml); tokens `AppColorTokens.scrim`, `borderAccent` (per mode, 50% accent), `proteinAccent`, `carbsAccent`, `fatAccent`. The macro colors follow the Macronutrients design (protein lime `#C3FF3D`, carbs orange `#F97316`, fats cyan `#28D5E6`), which differs from the `macroProtein/Carbs/Fats` gradients in the Daily Goal bar. `CircularHeaderButton` gained `size`/`iconSize`/`backgroundColor`. The private accent button in `NutritionActionButtons` was extracted into a shared `PillButton` (`config/widgets/buttons/custom_buttons/pill_button.dart`, `accent`/`neutral` variants, `isLoading`, ellipsizing label). Tests: `test/food_scan_result_view_model_test.dart`, `test/food_scan_result_sheet_test.dart` (edit → back → save, back-while-editing, close), and new model cases in `test/food_model_test.dart`. Saved scans show up on the Nutrition tab immediately through its live Firestore stream.
- The HealthKit capability is temporarily commented out so the app can be signed with a free Apple ID (personal team), which can't provision HealthKit. The two `com.apple.developer.healthkit*` keys in `ios/Runner/Runner.entitlements` and the `SystemCapabilities` → `com.apple.HealthKit` block in `ios/Runner.xcodeproj/project.pbxproj` are commented out. The Dart health code, the `health` package and the `Info.plist` usage strings are unchanged. On iOS, "Connect Apple Health" now fails gracefully with "Apple Health access was not granted". Android Health Connect is not affected. To re-enable HealthKit with a paid team, uncomment both, or add HealthKit back under Signing & Capabilities in Xcode.
- Full Nutrition feature (first functional pass — UI polish comes later). **Data:** every food log is a `FoodLog` (`models/food_log.dart`) — the `FoodModel` JSON plus `mealType` (`breakfast`/`lunch`/`dinner`/`snacks`) and `loggedAt` (UTC ISO string); logs saved before this change fall back to `createdAt` and a time-of-day meal, but only documents with `loggedAt` match day queries. Water is a separate `users/{uid}/water_logs/{id}` collection (`WaterLog`, new `AppCollection.waterLogs`). `NutritionLogService` (`services/nutrition_log_service.dart`) owns all reads/writes: live `watchLogs(from, to)` (food + water combined, single-field `loggedAt` range, so no composite index), `watchRecentFoodLogs`, `fetchFoodLogs`, add/delete food and water. Every screen subscribes to the stream, so anything logged in a sheet appears everywhere instantly. `FoodScanService.saveFood` was removed in favour of it. **Domain:** `NutritionDay` (`models/nutrition_day.dart`) aggregates a day (calories, macros, fiber, sugar, sodium, water = drinks + water from food, per-meal totals, `DayLogStatus` full ≥3 meals / partial / none) and scores **Flow Points: 25 per day across 9 `FlowCategory`s** — Calories 5 and Carbs/Fats 3 (full within ±10% of goal, zero beyond ±50%), Protein 5 / Water 3 / Fiber 2 (progress to goal), Meal Timing 2 (1 per two meals logged), Sugar 1 / Sodium 1 (at or under the limit). Food categories need at least one food log; water counts on its own. The week max is 175, which is what the Nutrition card, the past-day impact card, the info sheet and the weekly report all read, so their numbers always agree. Goals stay `NutritionGoal.defaults` (2,450 kcal · 200g P · 310g C · 90g F · 30g fiber · 50g sugar · 2,300mg sodium · 2,500ml water) until per-user targets exist. **Nutrition tab** (`NutritionViewModel` rewritten around the day stream): today always shows the full layout — "Start Tracking Today" card when nothing is logged, otherwise the Diet Plan card — plus Recent Food Logs (tap to re-log to the same meal, with an Undo snackbar), Calorie Summary ring, Macronutrients (tap → sheet), Water/Fiber/Sugar/Sodium tiles (Water → sheet), Meal Breakdown (tap a meal with items → Meal Details, empty meal or the bright "ADD MEAL" pill → Log Food), Nutrition Flow Points (ⓘ → info sheet), Weekly Nutrition Report. **Past days are read-only** (following the "Historical data (read-only)" design, so Flow Points can't be backfilled): they show the "How this day's nutrition contributed to your Flow Score" breakdown and the read-only pill, and hide Scan/Log Food, meal "+" buttons, deletes and water quick-add; empty past days keep the empty-state card. Future days allow pre-logging and keep the "Nothing logged yet" + Daily Goal + Plan Ahead layout until something is logged. **Sheets** (all on the shared floating sheet): Log Food (`views/log_food_sheet.dart`, `LogFoodViewModel`) searches a local 30-item `FoodCatalog` (`models/food_catalog.dart`, USDA-style values per serving, logged as `FoodSource.manual`), meal chips, + adds / ✓ marks foods already in that meal; Water Intake (`WaterIntakeViewModel`) with goal ring, Small/Glass/Bottle/Large quick add, custom ml (max 3,000), the day's log with swipe-to-delete and a "From meals" row; Macronutrients (`MacronutrientsViewModel`) with the day's calorie split, Protein/Carbohydrates/Fats tabs, grams vs goal, kcal/g, description, pro tips and recommended range (static copy on the `MacroNutrient` enum); Nutrition info (Flow Points explanation + Got it). **Screens** (shared `InnerPageScaffold`, `config/widgets/scaffolds/`): Meal Details (`AppRouter.mealDetails`, `MealDetailsArgs`) with meal header + macro split bar, Food Items (swipe to delete, Add Food Item → Log Food), a rule-based Nutrition Insight, and a Meal Timeline that switches meals in place; Weekly Nutrition Report (`AppRouter.weeklyNutritionReport`, Mon–Sun of the selected date) with FLOW Contribution + daily point bars, Consistency (days logged, calorie/protein streaks of consecutive days ≥70% of goal counted back from today, per-day full/partial/none dots), a Calories/Protein/Fiber bar chart with average over logged days and weekly total vs goal, the 9-category points breakdown, and a tip pointing at the biggest missed category; 30-Day WAVE Diet Plan (`AppRouter.dietPlan`): no AI backend yet, so `DietPlan.generate` (`models/diet_plan.dart`) deterministically builds 30 days × 4 meals from template meals scaled to the calorie goal. The start date is saved per user in `SharedPreferences` (`DietPlanService`) the first time the screen opens, and a day counts as completed when that date's logged calories reach 80% of the plan day. Days expand to show meals and daily totals. **Shared pieces added:** `AppProgressBar`, `AppProgressRing` (`config/widgets/progress/`), `AppChipTabs` (`config/widgets/tabs/`), `CardHeader` (`config/widgets/headers/card_header.dart` — `HomeCardHeader` moved here with optional `icon`/`trailing`, Home cards updated), optional `footer` on `AppSheetPanel`, optional `icon` on `NutritionTipCard`, `AppDateUtils.atTimeOf/shortWeekday/monthDayYear/time/relativeDay`, `Vitamins.zero`/`Minerals.zero`, and `FoodModel.copyWith(id:, createdAt:)`. **One macro palette everywhere:** protein lime `#C3FF3D` (`proteinAccent`), carbs orange `#F97316` (`carbsAccent`), fats cyan `#28D5E6` (`fatAccent`), fixed across all three visual modes — the `macroProtein/Carbs/Fats` gradients ramp from the same three accents. **Backend:** `floww_cloud/firestore.rules` gained a `users/{uid}/water_logs` owner-only block (redeploy with `firebase deploy --only firestore:rules`, otherwise water logging fails with permission-denied). **Stats always follow food:** `NutritionLogService.combineLogs` emits food as soon as it arrives and treats a failing water query (e.g. rules not deployed) as "no water" instead of blocking the whole day — previously a denied water query meant the day stream never emitted, so scanned meals saved but every stat stayed at 0. Unreadable log documents are skipped (`parseDocuments`) rather than breaking the stream, and `loggedAt` is written at fixed millisecond precision (`AppDateUtils.isoKey`) so day-range string queries compare correctly. **Day-wise:** every screen queries only the selected day, and `DayRolloverTimer` (`config/utils/dates/`) moves "today" to the new date at midnight — the Nutrition tab jumps forward if it was showing the day that just ended, and Home re-subscribes — so today's meals never carry into tomorrow's stats (only the Recent Food Logs quick-add row intentionally lists earlier foods). **Home tab:** `HomeProvider` now takes `NutritionLogService` and watches today's logs, so the Nutrition Summary card (calories, protein, carbs, fats), the Water Intake habit (drinks + water from food vs the 2.5 L goal) and Today's Progress (Habits count, Nutrition complete at ≥90% of the calorie goal) update live; the other Home cards are still sample data. **Not done yet:** the WAVE orb, Plans and edit-an-existing-log flows are untouched. **Tests:** `nutrition_day_test.dart` (totals, status, all 25 points, partial scoring, limits), `diet_plan_test.dart`, `weekly_report_view_model_test.dart`, `log_food_view_model_test.dart`, plus the scan-result tests ported to the new save path, all against `test/fakes/fake_nutrition_log_service.dart`.
- Smooth (iOS / Figma "corner smoothing") corners everywhere via the `smooth_corner` package. All rounded shapes go through `AppShapes` (`lib/config/theme/app_shapes.dart`, smoothness `0.6` — Figma's closest match to iOS): use `AppShapes.decoration(borderRadius:, color:, gradient:, image:, side:, shadows:)` instead of `BoxDecoration(borderRadius: ...)` (note `border: Border.all(...)` becomes `side: BorderSide(...)` and `boxShadow` becomes `shadows`), `AppShapes.border(...)` instead of `RoundedRectangleBorder`, `SmoothClipRRect(smoothness: AppShapes.smoothness, ...)` instead of `ClipRRect`, and the new `SmoothInputBorder` (`lib/config/widgets/text_field/smooth_input_border.dart`) instead of `OutlineInputBorder` for text fields. All 65 rounded decorations, 6 clips, 6 text-field borders and the date-picker sheet shape across auth, onboarding, home, nutrition, navigation and shared widgets were converted. Pills keep working because the radius is clamped to half the shortest side, and animated shapes (chip tabs) still lerp. True circles (`BoxShape.circle`) are unchanged — smoothing a circle is visually identical. Two behaviour notes: `ShapeDecoration` does not inset content by the border width the way `BoxDecoration` did (a 1–1.5px difference on bordered boxes), and it asserts that `color` and `gradient` are not both set — `FlowScoreCard` had both (the gradient fully covered the color), so its hidden `color` was removed.

- `InnerPageScaffold` (`lib/config/widgets/scaffolds/inner_page_scaffold.dart`) now renders a real header instead of the placeholder back arrow it shipped with: the shared `CustomHeader` inset by `context.sizes.screenHorizontalPadding`, so every inner page gets the centred title with the circular `CircularHeaderButton` back chevron on the left, wired to the `onBack` callback (it previously did nothing). Scroll content also clears the header by `AppSpacing.md`. This matches the 30-Day WAVE Diet Plan design and moves Meal Details and the Weekly Nutrition Report with it.
- Water Intake sheet (`lib/core/nutrition/views/water_intake_sheet.dart`) matched to its design. Two shared widgets changed, so every screen that uses them moved with it. `AppSheetPanel` (`lib/config/widgets/sheets/app_sheet_panel.dart`): the subtitle now sits tight under the title (`AppSpacing.xxs` instead of `xl`) in `textSecondary`, and the gap before the scrollable body grew to `AppSpacing.xl` — this is the header for the Water Intake, Macronutrients, Log Food, Nutrition Points, food-scan review and meal-edit sheets. `NutritionTipCard` (`lib/core/nutrition/widgets/nutrition_tip_card.dart`) gained a second, compact form for sheets: `NutritionTipCard.inline` is a single `Text.rich` run — a 16px accent icon, the accent title, then the message inline in `bodySmall`/`textSecondary` — used by the Water Intake sheet, the Nutrition tab and the Weekly Report. The default constructor keeps the 32px gradient icon disc with the title and message beside it, which is what Meal Details uses.

- Meal Details screen (`lib/core/nutrition/views/meal_details_view.dart`) matched to its design screenshots. `MealSummaryCard` now stacks the macro block the way the design does — the coloured Protein/Carbs/Fats legend above the split bar and the `42g 25%` amount/share pairs below it (extracted into `_MacroLegendRow` and `_MacroValuesRow`) — instead of one label+value column under the bar. `MealTimelineCard` shows the header chevron (`CardHeader(showChevron: true)`). The Add Food Item button reads `ADD FOOD ITEM` with an accent-coloured `Icons.add_circle`, which is why `PillButton` (`config/widgets/buttons/custom_buttons/pill_button.dart`) gained an optional `iconColor` that overrides the variant foreground for the icon only. **The macro palette changed to match the screenshots:** protein cyan `#28D5E6` (`proteinAccent`), carbs orange `#F97316` (`carbsAccent`), fats violet `#8B5CF6` (`fatAccent`), with the `macroProtein/Carbs/Fats` gradients ramping from the same accents (`#00C4D5` and `#6D28D9` as the mid stops). Protein was lime and fats were cyan before, so every screen reading macro colours — Daily Goal, Macronutrients, the scan review, the Weekly Report — moves with it. Food-item rows keep their icon avatars; the design's food photographs are not wired up (no assets for them yet).
- Activity tab (`lib/core/workout/`) replaces the old `FeaturePlaceholderView`. It is a single screen with a horizontally scrollable pill tab bar (`WorkoutTabBar`) over four `WorkoutTab` values — Overview, History, Programs, Exercises — driven by `WorkoutViewModel`, which also owns the date shown in the header (same day-navigation + `DayRolloverTimer` rollover behaviour as `NutritionViewModel`). Every tab renders `WorkoutEmptyStateCard` (glow card, centred icon/title/message) with copy from `WorkoutViewModel.emptyState`; Overview additionally shows `SuggestedWorkoutCard` when the selected date is today, and Programs shows `ProgramListSection` under an "ALL PROGRAMS" label. `WorkoutService` supplies the suggested workout and the three seed programs as static data — there is no workout backend yet, so `Start Workout` and the per-program `Start` buttons currently only fire a haptic; wiring them to a real session flow is the next step. The Exercises tab had no design screenshot, so it reuses the shared empty-state card with its own copy.
- Workouts **Overview** tab built out from its design frame (`lib/core/workout/`). `WorkoutViewModel` now defaults to `WorkoutTab.overview` and exposes a `WorkoutOverviewItem` (`overview`), mapped from a new `WorkoutSession` domain model that `WorkoutService.completedSessionFor(date)` returns as static sample data (there is still no workout backend). All formatting — `48:20` from seconds, `12,450` via `NumberFormatter.grouped`, `85%` shares, the `0m/12m/24m/36m/48m` chart axis, the filled-segment count — happens in the view model, so the widgets stay UI-only. The tab renders `WorkoutOverviewSection`: `WorkoutSummaryCard` (gradient hero card, icon tile, live-dot exercise count, Flow points, Completed chip, and a 2×2 stat grid of Duration/Volume/Calories/Avg Heart Rate split by 56px rows and 1px dividers), `TrainingEffectCard` (4.2 / Great, summary copy, a 7-segment `SegmentedLevelBar`, recovery hours), `MuscleFocusCard` (the existing `AppImages.muscleFront`/`muscleBack` SVGs beside four `AppProgressBar` rows, plus a View Anatomy chip), `HeartRateCard` (Average/Peak/Zone tiles over a `HeartRateChart` — a `CustomPainter` drawing the area fill, line and per-sample dots), and `PerformanceCard` (New PRs/Exercises/Sets/Reps, an orange-outlined Personal Records panel, and a full-width bright `VIEW WORKOUT DETAILS` pill). The empty state now only shows when there is no overview to render, and `View Anatomy`/`View Workout Details` are haptic-only placeholders like the existing `Start Workout`.
- The Heart Rate and Performance cards were then matched to their own frames. Heart Rate stat tiles are `backgroundSurface` (`#1F1F1F`) at `AppRadius.lg` with a `borderSubtle` outline, 56px tall, 10px padding, and three *different* value colours — Average `#DC2626` (`destructive`), Peak `#BAFF1F` (`primaryAlt`), Zone `#EA580C` (new `accentOrangeDeep`), which is why `WorkoutStatTone` gained an `ember` case distinct from `warning` (`#F97316`, the New PRs colour). The sparkline fill is the accent at 35%, the line is `primary` (`#C3FF3D`) at 2.07px, and dots are drawn only at the axis-label positions (6 markers over 11 samples) rather than at every vertex; axis labels are `space-between`. The Personal Records panel is a banded, clipped container — an amber header strip over `#1F1F1F` rows separated by a `borderSubtle` top border, each row 42px tall with a pill badge — instead of a plain bordered box, and record labels carry the frame's emoji glyphs (`PersonalRecord.glyph`), so `PersonalRecordItem` holds a composed `label` string rather than an `IconData`. `VIEW WORKOUT DETAILS` is a 32px `PillButton.bright` with `captionSemiBold` text and a 16px `Icons.arrow_circle_right` in `onSurfaceBright`.
- Programs tab built out. `ActiveProgramCard` is the same gradient hero treatment as `WorkoutSummaryCard` (`darkGlow` + `cardSheen`): icon tile, name over "Week 4 of 12 · 4 days/week", a filled lime `ACTIVE` chip, an `AppProgressBar` and a level / percent-complete footer. `WorkoutChip` grew from an `isAccent` bool to a `WorkoutChipTone` enum (`muted`/`accent`/`filled`) to supply that solid badge. Tapping a program row opens `ProgramStartSheet` — a `showAppFloatingSheet`/`AppFloatingSheet` sheet (blurred, dimmed backdrop, the same chrome the Nutrition sheets use) with a lime-tinted icon tile, name + description, a `borderGlow` stats box of Duration / Frequency / Level split by dividers, an orange-outlined warning shown only when an active program exists, and an accent `PillButton` reading "Start <program>". The stats box reuses `WorkoutStatTile`, which gained an optional `titleStyle` since the sheet's labels are sentence-case body text rather than the 10px caption used on the Overview. `WorkoutService` gained `description` on `WorkoutProgram`, an `ActiveProgram` model with `loadActiveProgram()`, and `programById()`; the view model derives the schedule/level/progress strings and the `ProgramDetailItem`. The empty-state card no longer shows on Programs.
- Exercises tab built out as its own module: `Exercise` + `MuscleGroup`/`Equipment` enums (`models/exercise.dart`), a seed `ExerciseService` catalog, and `ExerciseLibraryViewModel` (registered beside `WorkoutViewModel` via `MultiProvider` in `main_tab_view.dart`) owning the search query, which group is expanded, per-exercise added state and the create-exercise draft. `ExerciseLibrarySection` renders `ExerciseSearchBar` (pill search field plus a lime-outlined circular + button), a lime "N custom exercises added" line, and a `MuscleGroupCard` per group — icon tile, name, "3 Exercises", chevron, and when expanded a divided list of rows with a `CUSTOM` badge and a check/plus toggle. The + opens `CreateExerciseSheet`: name field, `SelectableChip` wraps for muscle group and equipment, and an "Add to Library" `PillButton` that stays disabled until the name is non-empty. Searching auto-expands every group that still has matches.
- Past dates render the Overview as read-only, reusing the treatment Nutrition already had rather than inventing one. `WorkoutViewModel` gained `isReadOnly` (`dayStatus == past`), `showReadOnlyBanner` and `readOnlyLabel` ("Yesterday's session — historical data (read-only)", falling back to a weekday name within a week and `dayMonth` beyond it). The Overview then shows a `TipCard` banner (the flame-in-a-circle glow layout) and wraps the whole block in `LuminosityLayer`, the existing greyscale `ColorFilter.matrix`, so every lime accent — the 4.2 score, the training-effect segments, the muscle bars, the View Anatomy chip — desaturates to white/grey exactly as on a past Nutrition day. `WorkoutOverviewSection` takes `showSummary`, and the gradient `WorkoutSummaryCard` is omitted on past dates.
- `NutritionTipCard` was promoted to `config/widgets/cards/tip_card.dart` as `TipCard` now that Activity uses it too, following the same rule that moved `SectionLabel` and `DateSelectorPill` out of `core/nutrition/widgets/`. All four existing call sites (Nutrition, Meal Details, Water Intake, Weekly Report) were renamed with it; the three named layouts (`glow`/`inline`/`stacked`) are unchanged.
- History tab built from its screenshot (`WorkoutHistorySection` + `WorkoutHistoryCard`). Under a `SectionLabel` ("RECENT SESSIONS") it lists `WorkoutHistoryItem`s: workout name over a date line, the training-effect score in `accentOrange` with an "effect" caption right-aligned, and a metric row (duration / kcal / exercise count) with an optional `WorkoutChip` status on the right. Today's session renders as `AppCardVariant.highlighted` (the `bgTinted` + `borderGlow` treatment) while older ones are `plain`, and the whole card is wrapped in `PressScale`. Data comes from `WorkoutService.loadHistoryFor(date)` as a new `WorkoutHistoryEntry` list (still static sample data); the view model formats every string — `Today · 8:30 PM` for today via `AppDateUtils.time`, otherwise `July 16` via a new `AppDateUtils.monthDay` (full month names, which the utils didn't have). `showEmptyState` is now `!showOverview && !showHistory`, so History no longer falls back to the empty-state card when sessions exist. `WorkoutMetricItem` (icon + label) was added alongside `WorkoutStatItem`, whose title/unit split doesn't fit these rows.
- Future dates now offer a suggestion instead of a dead end. `WorkoutViewModel.suggestion` is gated on `dayStatus != past` (it was `== today`), so today *and* any future day return one; the Overview tab still resolves `overview` to null for future dates, so a future day renders the empty-state card plus `SuggestedWorkoutCard`. The card's header is no longer the hardcoded "Suggested For Today" — it takes a `title`, fed by the new `WorkoutViewModel.suggestionTitle` ("Suggested For Today" on today, "Suggested For Saturday" on any other day, via `AppDateUtils.weekdayName`). The Overview empty state is date-aware too: on a future day it uses a calendar icon and "This is a future date. Come back on <weekday> to log your workout, or schedule one now." instead of the first-session copy.
- Tokens added for that screen rather than inlining design values: `AppSizes.s108`; `AppTypography.bodyXXLargeBold` (Hanken 40/48, the 4.2 score), `bodyMediumMediumTight` (14/20), `bodyXSmallRegular` (10/12 units) and `captionSemiBold` (Plus Jakarta 10/12, the caption style the frame uses everywhere); colours `textQuiet` (`#919191`, the frame's caption grey), `accentOrangeDeep`, `accentOrangeLight` (`#FB923C`), `amberBorder` (`#F59E0B` at 24%) and `amberSurface` (at 6.3%) — all mode-independent, so they default in `AppColorTokens` and need no per-mode entries; gradient tokens `chartArea` (per-mode accent area fill at 35%) and `cardSheen` (the frame's `#1D1D1D` radial highlight anchored top-right). **`gradients.darkGlow` was flipped** to `topCenter→bottomCenter` so it matches the frame's `linear-gradient(180deg, #1C2218 0%, #93D500 100%)` — it previously ran bottom-to-top and had no other users. The shared `CardHeader` gained optional `titleStyle`, `titleTrailing` (the inline ⓘ glyph), `iconColor` and `iconSize`; all four are optional, so existing headers are unchanged.
- Header and label widgets were promoted out of `core/nutrition/widgets/` now that two features use them: `DateSelectorPill` → `config/widgets/headers/date_selector_pill.dart`, `NutritionSectionLabel` → `config/widgets/headers/section_label.dart` as `SectionLabel`, and `NutritionHeader` was replaced by `config/widgets/headers/screen_date_header.dart` (`ScreenDateHeader`), which takes the screen title as a parameter ("Nutrition" / "Workouts") instead of hardcoding it.
- `FlowContributionCard` (Weekly Report) was matched to its design frame: a flat `#181818` surface with a 1px `borderSubtle` outline and a 16px radius instead of the lime `glowCard` gradient at radius 24. `AppCard` gained an `AppCardVariant.subtle` (background `backgroundSecondary`, `borderSubtle` border) and an optional `radius` parameter that defaults to `AppRadius.xl`, so no other card changes. A `borderSubtle` `Divider` separates the progress block from "Daily Breakdown", and `WeeklyBarChart`'s value badges shrank (height `AppSizes.s6`, `labelSmall` text, `AppSpacing.md` horizontal padding) — this applies to the Nutrition FLOW Points chart too, since both charts share the widget.
- `ConsistencyCard` matched to its design frame: the card is `AppCardVariant.subtle` (`#181818`) at the default `AppRadius.xl` (24) and the three stat tiles moved to `backgroundSurface` (`#1F1F1F`) at `AppRadius.xl` instead of `backgroundElevated` at `AppRadius.md`, so tiles and card share the same corner language. Tile padding is `AppSpacing.xl` vertical with `AppSpacing.xs` gaps between value / label / caption, which reproduces the 92px tile height from the spec.
- `WeeklyMetricCard`, `FlowPointsBreakdownCard` and the metric `AppChipTabs` matched to their design frames. The metric card is no longer an `AppCard`: it is a `backgroundPrimary` (`#0A0A0A`) surface at `AppRadius.lg` with `AppSpacing.lg` padding and a 1px border in the *metric's own* colour at `AppOpacity.tintBorder` (new `lib/config/constants/app_opacity.dart`, `tintFill` .08 / `tintBorder` .24), so the outline follows Calories/Protein/Fiber. Its chart area grew to `AppSizes.s160` (new token) and all the 16px numbers use `AppTypography.bodyLargeBold` instead of `headlineSmall`. `FlowPointsBreakdownCard` and `FlowContributionCard` both use a new `AppCardVariant.accentOutline` (`backgroundSecondary` + the existing `borderGlow`, which is already the accent at 24%). Points rows: `AppSpacing.lg` between rows (none above the first or below the last), a 4px `AppProgressBar`, 12px points label, and the `max n/wk` hint in a new `textFaint` token (`0x59FFFFFF` — 35% white, between `textSecondary` 50% and `textTertiary` 20%). `WeeklyBarChart` only reserves badge headroom when a bar actually has a badge, uses `AppSpacing.xxs` bar gaps and `AppSpacing.sm` under the bars. The inline `NutritionTipCard` moved to `AppRadius.lg` with 8/12 padding and all-accent text — this also changes the tip on the Nutrition tab and the Water Intake sheet, which share that widget. `AppChipTabs` chips are now padding-sized (6/12) with a `hairline` (0.5px) border, `backgroundPrimary` when unselected, and a semibold label when selected.
- Top scroll blur (content fading into a blur under the status bar / pinned header) is now a shared widget: `TopProgressiveBlur` (`lib/config/widgets/effects/top_progressive_blur.dart`) wraps the `progressive_blur` package with the app's own tokens. The blurred band is measured from the real header instead of a fraction of the screen — `MediaQuery.viewPaddingOf(context).top + kToolbarHeight + topBlurBandExtra` (4) — and a `LayoutBuilder` converts it into gradient stops, so everything above the line is fully blurred (`topBlurSigma` 24) and everything below it is sharp — the gradient holds full strength across the whole band and drops to zero over a `topBlurEdgeFade` (2) hairline, which the divider sits on top of. `topBlurTextureSize` (512) raises the blur-strength texture resolution so the short fade doesn't band. Only `InnerPageScaffold` uses it (Meal Details, Weekly Report, Diet Plan); the tab screens are deliberately left unblurred. A full-bleed 1px `borderSubtle` `Divider` closes off the bottom of the band, positioned from the same `TopProgressiveBlur.bandHeightOf(context)` the gradient is built from, so the line and the end of the blur can't drift apart. `InnerPageScaffold` became a `StatefulWidget` to own the `ScrollController` behind it: the line is hidden at rest and fades in over 180ms once the list has scrolled past `AppSpacing.md`, so a screen that opens at the top has no line under its header. The scroll offset drives a `ValueNotifier` read by a `ValueListenableBuilder`, so scrolling repaints the divider only, not the page. The blur is drawn by a fragment shader that `ShaderBuilder` loads asynchronously — until it resolves the child renders unblurred, which is why the effect looked missing — so `main()` now awaits `ProgressiveBlurWidget.precache()` before `runApp`. **A hot reload will not pick this up; hot restart or relaunch the app.**
- Meal Details polish pass against the latest design frame. **Add Food Item** is now the design's bright pill: `PillButton` gained a `bright` variant (background `surfaceBright` `#E0EBE2`, label in `bgWarm` `#23221A`, no border, a soft double lime shadow at `AppOpacity.softGlow`) plus optional `height` and `labelStyle`, so the button renders at `AppSizes.s36` with `AppTypography.labelSmallSemiBold` (Plus Jakarta Sans 12/600) and an `onSurfaceBright` (`#659100`) `Icons.add_circle`; when a `labelStyle` is given the icon drops to 16px and the icon/label gap to `AppSpacing.xs`. New per-mode tokens `AppColorTokens.surfaceBright` and `onSurfaceBright` (steady `#EBE4E0`/`#8C3F00`, restore `#E0EAEB`/`#00697A`). **Nutrition Insight** moved from the flat tinted card to `AppCardVariant.innerGlow`: a `#181818` (`backgroundSecondary`) surface with a `borderSubtle` outline and a green inner rim glow, drawn by the new shared `InnerGlow` (`config/widgets/effects/inner_glow.dart`) — a `CustomPainter` that clips to the card's `AppShapes` path and strokes it with a `MaskFilter.blur`, since Flutter has no inset box shadow. Glow strength lives in `AppOpacity.innerGlow` (0.18). The card's title now uses `primaryAlt` and the message `textPrimary`, with the gradient icon disc at 16px. `MealTimelineCard`'s four meal stats are centred inside their equal-width columns instead of left-aligned.
- Past-day Nutrition tab matched to the "Nutrition - Date Change (Past)" design frame. **Every past date** now leads with the Flow Score Impact card (`showFlowImpact` is `dayStatus == past`, no longer gated on the day having logs), so a past day with nothing logged still shows the card with `+0 Flow Points` — `FlowImpactCard` drops the breakdown list and its spacing when there are no rows. The card is an `AppCardVariant.accentOutline` surface (`#181818` + `borderGlow`) holding the ⚡ header "How this day's nutrition contributed to Flow Score Impact", then the max-points box: `tint` fill, `borderGlow` outline, `AppRadius.lg`, the sentence "Max possible from nutrition:" over `+25 Flow` (accent) `per day`, a 40px `borderMedium` rule, and the day's total over "Flow Points". `NutritionViewModel.flowImpactMaxLabel` is now just `+25 Flow` so the widget can colour that span inside the sentence. **Only categories with data that day get a row** (`_hasFlowData` — calories/macros/fiber/sugar/sodium/water above zero, or at least one meal logged), so an empty past day shows no rows instead of nine zeroed ones. Row styling in `FlowPointsRowList` (shared with the Weekly Report breakdown): Plus Jakarta 12/16 label, 9px `captionMedium` detail in `textFaint`, `primaryAlt` points, 1px `borderSubtle` dividers between rows, and the 4px `AppProgressBar` on a `borderSubtle` track. The old `ReadOnlyPill` was deleted in favour of the design's Wave Note Card — the existing `NutritionTipCard` (its `message` is now optional and the icon disc uses `gradients.primary`) with a flame icon and the "Yesterday's — historical data (read-only)" label. Everything from that note card down (empty state, calorie summary, macros, tiles, meals, weekly report) renders inside the new shared `LuminosityLayer` (`config/widgets/effects/luminosity_layer.dart`, a saturation-zero `ColorFilter.matrix`), which is the design's `mix-blend-mode: luminosity` group — past-day content reads as greyed history while the impact card keeps its lime. Today and future days are unaffected (`enabled: false`). New typography tokens for the frame: `labelLargeSemiBold`, `labelSmallMedium`, `labelSmallRegular` (Plus Jakarta 16/12) and `bodySmallMediumTight`, `bodyXLargeBold`, `captionMedium` (Hanken Grotesk).
- Card surfaces darkened to match the design frames: `AppCardVariant.plain` (the `AppCard` default) is now a flat `backgroundSecondary` (`#181818`) with no border instead of `backgroundSurface` (`#1F1F1F`) plus a `borderSubtle` outline, which was reading grey against the `#14110B → #0A0A0A` screen gradient. `AppCardVariant.flat` was identical to the new `plain` and was removed — `DailyGoalCard` uses the default. In `RecentFoodLogsSection` the log tiles lost their `borderSubtle` outline (the design has none) and the meal chip moved from `backgroundElevated` (`#2D2D2D`) to `backgroundSurface` (`#1F1F1F`) with a `borderSubtle` hairline, as specified. Floating sheets invert that stack, matching the Macronutrients frame: `AppFloatingSheet`'s panel stays `backgroundSecondary` (`#181818`) but lost its `borderSubtle` outline (the design has no stroke on the sheet), and the cards inside it go *darker* than the panel. New `AppCardVariant.sunken` (`backgroundPrimary` `#0A0A0A` + the accent `borderGlow`) carries `MacroDetailCard`, whose PRO TIPS box flipped the other way to `backgroundSecondary` so it reads one step above the card, and whose macro icon tile is `backgroundSecondary` with a `borderGlow` ring instead of `backgroundElevated` (`#2D2D2D`). The sheet close button is `backgroundPrimary` so it stays visible on the `#181818` panel. Macro accents were re-pointed at the frame's colours: `proteinAccent` is now the lime `#C3FF3D` (was cyan) and `fatAccent` the cyan `#28D5E6` (was violet `#8B5CF6`), with `carbsAccent` staying orange `#F97316` — this flows through the split bar, the macro labels, the detail card and every other `MacroNutrient.colorOf` consumer. `AppCard` gained an optional `borderColor` override so `MacroDetailCard` can outline itself (and its icon tile) in the *selected macro's* colour at `AppOpacity.tintBorder`, the same treatment `WeeklyMetricCard` uses, instead of the fixed lime `borderGlow`. The Water Intake sheet picked up the outlines its frame shows: the goal-ring summary card is `AppCardVariant.sunken` (near-black with the lime `borderGlow`), the Quick Add card is `AppCardVariant.subtle` (`#181818` with a `borderSubtle` hairline), Today's Log is `sunken` with its border overridden to `borderSubtle` (near-black like the summary card, grey outline instead of lime), and the quick-amount buttons fill with `backgroundPrimary` behind their existing `borderSubtle` outline so they read as recessed against the card. The Hydration Tip uses a new `NutritionTipCard.stacked`: the same tinted card and lime border as `.inline`, but `AppSpacing.xl` padding with the icon and lime title on their own row above a `textSecondary` message, as the frame shows — `.inline` keeps its compact all-accent single line for the Weekly Report tip. `MacroSplitCard` drops to `AppRadius.lg` (16) from the default 24, and the "Recommended:" pill gained a `tintFill` (0.08) wash in the macro's colour behind its existing outline.
- The Activity feature module was renamed to Workout: `lib/core/activity/` is now `lib/core/workout/`, keeping the standard `models/` `services/` `view_models/` `views/` `widgets/` layout. Files renamed with it — `models/activity_tab.dart` → `workout_tab.dart`, `models/activity_view_data.dart` → `workout_view_data.dart`, `services/activity_service.dart` → `workout_service.dart`, `view_models/activity_view_model.dart` → `workout_view_model.dart`, `views/activity_view.dart` → `workout_view.dart`, `widgets/activity_tab_bar.dart` → `workout_tab_bar.dart`, `widgets/activity_empty_state_card.dart` → `workout_empty_state_card.dart` — and so were the symbols: `ActivityView` → `WorkoutView`, `ActivityViewModel` → `WorkoutViewModel`, `ActivityService` → `WorkoutService`, `ActivityTab` → `WorkoutTab`, `ActivityTabBar` → `WorkoutTabBar`, `ActivityDayStatus` → `WorkoutDayStatus`, `ActivityEmptyState` → `WorkoutEmptyState`, `ActivityEmptyStateCard` → `WorkoutEmptyStateCard`. `main_tab_view.dart` is the only file outside the module that imports it. The bottom-tab label is still "Activity" — this was a code move, not a copy change — and the unrelated onboarding `OnboardingGoalsActivityDetails`/`activityLevel` entity fields were left untouched.
- The Overview ⓘ glyphs and the View Anatomy chip are wired up. The two info glyphs (Workout Summary, Training Effect) open the "Got it" explainer sheet from the design frames — that sheet is now shared as `AppInfoSheet` (`config/widgets/sheets/app_info_sheet.dart`, title + `Icons.question_mark_rounded` badge + message + centred `PillButton`), extracted from the Nutrition-only `NutritionPointsSheet`, which was deleted; the Nutrition tab passes `title: 'Nutrition'` and gets the same sheet it had. `WorkoutSummaryCard` and `TrainingEffectCard` take an `onInfo` callback handed to `CardHeader.onTap` (the glyph itself is unchanged), and `WorkoutOverviewSection` forwards `onSummaryInfo`/`onTrainingEffectInfo`. Copy comes from `WorkoutViewModel.summaryInfoMessage` (date-aware — "Today's overview…" / "Saturday's overview…") and `trainingEffectInfoMessage`. **View Anatomy** opens `MuscleAnatomySheet` (`core/workout/views/muscle_anatomy_sheet.dart`): an `AppFloatingSheet`/`AppSheetPanel` titled "Muscle Anatomy" over "Today's activation map", a `sunken` card holding the FRONT/BACK figures at `AppSizes.s160`, an "ACTIVATION DETAIL" `subtle` card listing every muscle (name, %, bar, `borderSubtle` dividers) with a lime ⚡ "Highest activation today" line under the top one, and a `TipCard.stacked` Recovery Tip. Data is `WorkoutViewModel.anatomy` → `MuscleAnatomyItem`, which sorts activation descending, marks the highest entry via the new optional `MuscleFocusEntry.highlight`, and composes the tip from two new `WorkoutSession` fields (`muscleRecoveryMinHours` 48 / `muscleRecoveryMaxHours` 72) — `recoveryHours` still drives the Training Effect card. Two widgets were extracted so the sheet and `MuscleFocusCard` share one implementation: `MuscleFigures` (the front/back SVGs, optional `labelled` layout) and `MuscleActivationRow` (name / percent / bar / optional highlight). `AppSheetPanel` gained an optional `titleStyle` for the sheet's larger heading.
- Workout Details screen built from its design frames (`core/workout/views/workout_details_view.dart`, route `AppRouter.workoutDetails` = `/workout_details`, argument: the workout id). It is reachable from the Workouts tab in two places: tapping any card in the History tab (`WorkoutHistorySection.onOpen`) and the bright `VIEW WORKOUT DETAILS` pill on the Overview's Performance card (`onViewDetails`, null-safe via the new `WorkoutViewModel.overviewWorkoutId`, which resolves the history entry for the selected date). The screen is an `InnerPageScaffold` titled "Workout Details"; `InnerPageScaffold` and `CustomHeader` gained an optional `onMore`/`onMorePressed` that renders the design's trailing `…` `CircularHeaderButton` (shown only when no close button is set). Content: `WorkoutDetailSummaryCard` (app-icon tile in lime, workout name over `Jun 23, 2026 at 8:30 PM` via `AppDateUtils.monthDayYear` + `time`, a `Completed ✓` `WorkoutChip`, and the 2×2 Duration/Volume/Calories/Avg-Heart-Rate grid built from the shared `WorkoutStatTile` with a full-height vertical divider between columns); `WorkoutExercisesCard` (header + "N exercises", then one accordion row per section — `🔥 Warm Up` / `⚡ Main Exercises` / `🌊 Cool Down` — with a status indicator per `WorkoutSectionStatus` (lime check ring / filled lime dot / dark dot), a chevron that flips between `chevron_right` and `keyboard_arrow_down`, and, when expanded, exercise rows with a circular thumbnail, `2 sets x 15 reps`, `BW Weight` + `0:45 min Rest` metrics and the right-aligned `kg vol.` value — bodyweight moves show `—`; a full-width `ADD EXERCISE` bright pill closes the card); the WAVE Insight `TipCard`; and `WorkoutNotesCard` (underlined lime "Edit" link over a sunken bordered notes box). New domain models live in `models/workout_detail.dart` (`WorkoutDetail`, `WorkoutSectionGroup`, `WorkoutExercise`, `WorkoutSectionStatus`, with `volumeKg` = weight × sets × reps), served as static sample data by `WorkoutService.detailFor(id)`; `WorkoutDetailsViewModel` owns which section is expanded (single-expand accordion, Warm Up open by default) and every formatted string, so the widgets stay UI-only. Not yet wired: the `…` menu, `ADD EXERCISE` and `Edit` are haptic-only placeholders like the rest of the module's actions, and the exercise thumbnails are a `fitness_center` glyph on `backgroundElevated` because the design's per-exercise photos are not in `assets/`. `test/workout_details_shot_test.dart` renders the screen full-height and asserts it lays out without overflow.
- Today's Workout + live workout session flow (`core/workout/`). **Today's Workout** (`views/todays_workout_view.dart`, route `AppRouter.todaysWorkout` = `/todays_workout`) is reached from the Workouts tab's `Start Workout` pill and the Home screen's `TodayWorkoutCard`, replacing the haptic-only placeholders. It is an `InnerPageScaffold` — which gained an optional `footer` that pins a widget above the safe area and reserves list padding for it — holding `TodaysWorkoutSummaryCard` (app-icon tile, `Leg Day` over `Strength Builder Program · Week 4`, a `borderGlow` stats box of Duration / Focus / Recovery built from the shared `WorkoutStatTile`, and a `TipCard.inline` "Today's Goal"), `TodaysWorkoutExercisesCard` (header + "6 exercises" over divided exercise rows) and a `TipCard.note` carrying the WAVE session note. The exercise row markup was extracted out of `WorkoutExercisesCard` into a shared `WorkoutExerciseRow` so both cards render it from one place. **Start Workout** pushes `ActiveWorkoutView` (`views/active_workout_view.dart`, route `AppRouter.activeWorkout` = `/active_workout`): a pinned header of back / elapsed-time pill (`CustomHeader.isTimerMode`) / red close, an `ActiveWorkoutProgressHeader` (overall set progress bar with `Exercise 1/6` and `Set 1/4`), and a scrolling body of `ActiveExerciseHeroCard` (3:4 media card, scrim, exercise name and two `ActiveMetricPill`s — `1 / 4 sets` and `8 reps · 100kg`), `SetProgressDots` (lime check / current ring / dark pending), a 🎯 Reps-in-Reserve pill, and the four `ExerciseInfoSectionCard` accordions — Personal Best, Common Mistakes, Guidelines, Equipment Required — which turn lime and outline when open and list ✓ or ✗ rows per `ExerciseInfoTone`. `ActiveWorkoutActionBar` pins pause / `Complete Set` / skip-exercise at the bottom; completing a set swaps the body for `RestTimerCard` (REST, the counting-down seconds, `Skip Rest`) and the primary button for `Next Set`, and the rest timer advances the set — then the exercise — on its own. State lives in `ActiveWorkoutViewModel` (one-second ticker, elapsed time, current exercise/set, rest countdown, pause, which accordion is open); the data is `WorkoutService.todayWorkout()` → `TodayWorkoutData.legDay` (`models/today_workout.dart`), still static sample data. New tokens: `PillButtonVariant.primary` (filled accent, dark label, glow) and `.outline`, `TipCard.note`, `CircularHeaderButton.borderColor`, `AppTypography.displayNumeric` (72px, the rest countdown). Not yet wired: the exercise photos from the design are not in `assets/`, so the hero card and row thumbnails fall back to a `fitness_center` glyph — drop the images into `assets/images/`, declare them in `AppImages` and set `TodayWorkoutExercise.imagePath` to light them up.

- Today's Workout / live-session polish. The stats box on `TodaysWorkoutSummaryCard` had its inner glow dialled back from `AppOpacity.innerGlowIntense` at `AppSizes.s24` blur to `AppOpacity.innerGlow` at `AppSizes.s8`, so the lime reads as a thin rim on the border instead of bleeding into the card. `ActiveWorkoutView` builds its own `Stack` rather than using `InnerPageScaffold`, so it never had the footer blur the rest of the app gets — it now renders the same `BottomActionScrim` behind the action bar (`viewPadding.bottom + AppSizes.s72 + `sizes.bottomScrimExtra`), and the info accordions fade under it instead of showing through the `Complete Set` pill. The emoji glyphs on the exercise info accordions and the Reps-in-Reserve pill became Material icons: `ExerciseInfoSection.glyph`/`ExerciseInfoSectionItem.glyph` (`String`) are now `icon` (`IconData`) — `Icons.emoji_events_outlined` / `warning_amber_rounded` / `checklist_rounded` / `fitness_center` — tinted `primary` when the section is open and `textSecondary` when closed, and `ActiveMetricPill.glyph` is likewise `icon` (`Icons.track_changes` for Reps in Reserve). **Exercise photos** are live: `TodayWorkoutExercise.imagePath`/`ActiveExerciseItem.imagePath`/`WorkoutExerciseItem` now carry an `imageUrl`, fed by six remote photo constants in `AppImages` (`barbellBackSquat`, `romanianDeadlift`, `legPress`, `walkingLunges`, `legExtension`, `standingCalfRaise` — Unsplash CDN URLs with `w=900&q=70`), so the hero card and the row thumbnails show the real movement instead of a black `fitness_center` placeholder. A shared `RemoteImage` (`config/widgets/images/remote_image.dart`) does the loading: it takes a nullable url plus a fallback icon and size, and renders that icon on `backgroundElevated` while loading, on error, and when the url is null — so an offline device degrades to the old placeholder rather than a broken box. `android.permission.INTERNET` was added to the Android manifest, which debug builds inject automatically but release builds do not.
- The workout share sheet's five action buttons are live, and Stories/Strava now carry their real brand marks. `ShareTargetItem` gained an optional `assetPath` alongside its (now optional) `icon`, so `ShareTargetRow` renders `SvgPicture.asset` for the two brand targets and a themed `Icon` for the rest; the marks are new `AppImages.instagramIcon` / `AppImages.stravaIcon` SVGs (`assets/icons/`) — the Instagram gradient tile and the orange Strava chevron — drawn at `AppSizes.s28` inside the existing `backgroundElevated` disc, while Add an image / Download image / Share stay on `Icons.add_photo_alternate_outlined` / `Icons.download_rounded` / `Icons.share_outlined` to match the frames. Each card page in the pager is now wrapped in a keyed `RepaintBoundary` over a `ColoredBox(backgroundSecondary)`, and `WidgetCapture.toPngBytes` (`config/utils/share/widget_capture.dart`) renders the *currently visible* card to a 3x PNG. `ShareService` (`config/utils/share/share_service.dart`) owns the IO: `writeTempImage` (path_provider temp file), `shareImage` (share_plus `SharePlus.instance.share` with the PNG plus a `Push Day · 50 min · 13,300 kg flowwapp.com` caption) and `saveImageToGallery` (gal, into a `Floww` album, requesting photo access first), each failure surfacing as a typed `ShareException`. `WorkoutCompletionViewModel.handleShareTarget(target, capture)` switches on the target — Stories / Strava / Share all open the system share sheet with the card attached (the user picks Instagram or Strava there; no native Stories channel), Download image writes to the camera roll and reports "Saved to your photos.", and Add an image reuses the existing `PhotoLibraryService` picker, stores the bytes on the view model and jumps the pager to the photo card, where `WorkoutShareCard` swaps the dashed placeholder for the picked `Image.memory`. While a target is running the view model exposes `busyTarget`/`isSharing`, so that tile shows a spinner and the row stops accepting taps; the resulting message (or error) renders as a line of `bodySmallMedium` under "Share & get reposted!" and clears on the next action or page swipe. New packages (approved): `share_plus`, `gal`, `path_provider`; `NSPhotoLibraryAddUsageDescription` was added to `ios/Runner/Info.plist` and the capped legacy storage permissions to the Android manifest, both needed by gal — adding them means the app must be **cold-restarted**, not hot-restarted, or gal raises `MissingPluginException` and the new SVGs fail to load.
- Workout Details notes are editable. The underlined lime `Edit` link on `WorkoutNotesCard` now opens `EditNotesSheet` (`core/workout/views/edit_notes_sheet.dart`), an `AppFloatingSheet` built like `CreateExerciseSheet` — title/subtitle header with the round close button, an autofocused 4–8 line multiline `TextField` in a sunken `backgroundPrimary` box with a `borderSubtle` outline, and a `Save Notes` `PillButton`. It is a `StatefulWidget` only to own its `TextEditingController`, seeded from the current notes; saving trims the text and calls `WorkoutDetailsViewModel.updateNotes`, which holds an override `_notes` that `detail.notes` and the new `notes` getter read before falling back to the service's sample notes, so the card updates immediately (in-memory only — there is still no workout backend). The sheet's copy (`notesSheetTitle`/`Subtitle`/`Hint`/`SubmitLabel`, `notesEmptyMessage`) lives on the view model, keeping the view UI-only.
- Fixed the render assert that fired whenever the Add Exercise sheet opened. The two target rows in `add_exercise_sheet.dart` use `CrossAxisAlignment.stretch` to keep the `ValueStepper`s the same height, but they sit inside a `Column` in a `SingleChildScrollView`, so the cross-axis constraint is unbounded and the children were never laid out (`RenderBox was not laid out ... hasSize`, cascading up through `AppFloatingSheet`). Both rows are now wrapped in `IntrinsicHeight`, which bounds them to their tallest child and keeps the stretch behaviour.

## Strict Coding Rules every time to follow

- Follow current coding style 
- Dont ever try to write comments in any conditions, so writing comments is strickly pohibited
- No hardcoded values until only screen specific or components specific and not available in config — colors, spacing, dimensions, font sizes, radii, etc. Always pull from `config/constants` or `config/themes`.
- Use EdgeInsets.symmetric instead of EdgeInsets.fromLTRB or others until its important at all places.
- No hardcoded images — any image asset a screen needs must first be added to `assets/images/` (or `assets/icons/` for icon assets), then declared as a named constant in `AppImages` (`lib/config/constants/app_images.dart`), and only then referenced from that constant in a View/widget. Never reference an asset path string directly in a View.
- No inline `TextStyle` — every text (only until its screen or component specific) widget uses the global text theme (`app_text_theme.dart`).
- Reuse before creating — check `config/widgets` and the feature's own `widgets/` folder before building something new.
- Shared widgets (buttons, text fields, cards, loaders, dialogs used across features) go in `config/widgets`; single-feature widgets go in `core/<feature>/widgets/`.
- No duplicate UI — if the same element appears in two or more places, extract it into a reusable widget.
- No UI built inline inside variables — extract non-trivial widget trees into proper widget classes.
- Never rebuild native system UI — no custom status bar, home indicator, or gesture area; that's the OS's responsibility.
- Strict MVVM — Views render UI only, ViewModels own logic and state, Services own data/IO. No business logic in Views.
- All features live under `core/<feature>/{services,view_models,views,widgets}`; new features follow this structure automatically.
- Pixel-perfect UI — the provided images of design is the source of truth for spacing, color, typography, sizing, alignment, shadows, and layout.
- Every color, gradient, and text style must be pulled from the app theme system (`Theme.of(context)` + custom `ThemeExtension`s). Never hardcode raw `Color(0x...)` / `LinearGradient(...)` values inside a screen, widget, or component. If a value isn't in the token set, add it to the token set — don't inline it.
- Extract design tokens (colors, spacing, typography, radius, dimensions, shadows) into constants/themes instead of hardcoding them in widgets.
- Keep code modular, readable, and small — prefer composition over duplication.
- Consistent naming across files, classes, methods, and widgets.
- Prefer `const` constructors/widgets wherever possible.
- No new packages or architectural patterns without approval — reuse the existing stack and conventions.
- Please update README.md file each time after any feature updations and max size of CLAUDE.md should be 160 lines, add in README to follow